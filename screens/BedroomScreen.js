import React from 'react';
import { View, Text } from 'react-native';

const BedroomScreen = () => {
   return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
         <Text>Yatak Odası Test</Text>
      </View>
   );
};

export default BedroomScreen;
